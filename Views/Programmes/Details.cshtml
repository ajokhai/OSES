@model OSES.Models.OSES.Programme

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Programme</h4>
    <hr />
    <dl class="dl-horizontal">
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Name }) |
    @Html.ActionLink("Back to List", "Index")
</p>
